# Architecture

This document describes the architectural patterns and decisions for this starter template.

The application uses Next.js App Router with Server Components as the default rendering strategy. Supabase provides backend services including authentication, PostgreSQL database, and storage.
